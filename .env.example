# YouTube Downloader Environment Variables
# Copy this file to .env.local for development

# Next.js Configuration
NODE_ENV=production
PORT=3000

# Optional: Set custom timeout for video processing (in milliseconds)
# Default: 20000 (20 seconds)
# DOWNLOAD_TIMEOUT=20000

# Optional: Set maximum file size for downloads (in bytes)
# Default: No limit (Railway handles this)
# MAX_FILE_SIZE=104857600

# Optional: Custom User-Agent for YouTube requests
# Default: Chrome 120 User-Agent
# CUSTOM_USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"

# Railway automatically provides:
# - PORT (the port your app should listen on)
# - RAILWAY_ENVIRONMENT (production/staging)
# - Other Railway-specific variables

# No additional environment variables required for basic functionality